import React from 'react';
import {PlaceholderScreen} from '../PlaceholderScreen';

export function SecretEntryScreen() {
  return (
    <PlaceholderScreen
      title="Secret Entry"
      description="Use the configured secret gesture or calculator code to reach hidden apps."
      actionLabel="Enter secret"
    />
  );
}
