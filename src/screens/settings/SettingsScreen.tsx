import React from 'react';
import {PlaceholderScreen} from '../PlaceholderScreen';

export function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="Adjust theme, secret access, lock timing, and ad preferences."
      actionLabel="Open settings"
    />
  );
}
