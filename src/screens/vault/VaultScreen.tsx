import React from 'react';
import {PlaceholderScreen} from '../PlaceholderScreen';

export function VaultScreen() {
  return (
    <PlaceholderScreen
      title="Private Vault"
      description="Hidden apps and private content live here after secret access and authentication."
      actionLabel="Reveal vault"
    />
  );
}
